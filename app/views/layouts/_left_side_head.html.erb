<h5>大会</h5>
<table>
  <tbody>
    <% Convention.all.each do |convention| %>
      <tr>
        <td>
          <div style="font-size: 0.8rem;">
            <%= link_to convention_path(convention.id) do %>
              <%= convention.name %>
            <% end %>
          </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<div class="mt-5 pr-1 text-right"><%= link_to "大会一覧ページへ", conventions_path %></div>