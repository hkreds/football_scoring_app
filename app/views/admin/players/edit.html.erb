<h3 class="px-2" style="width:fit-content;background-color:#e4e4e4;">
  <strong><%= @player.name %></strong>の編集
</h3>
<div class="my-5">
  <%= form_with model: [:admin, @player] do |f| %>
    <%= render 'layouts/errors', obj: @player %>
    <table class="table table-borderless">
      <thead>
        <tr>
          <th>選手名</th>
          <th>生年月日</th>
          <th>身長(cm)</th>
          <th>体重(kg)</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><%= f.text_field :name, :size=>"35", placeholder:"選手名" %></td>
          <td><%= f.text_field :birthday, placeholder:"20220101" %></td>
          <td><%= f.text_field :height, placeholder:"180" %></td>
          <td><%= f.text_field :weight, placeholder:"75" %></td>
          <td></td>
        </tr>
        <tr>
          <td colspan="3"></td>
          <td class="text-right"><%= f.submit "変更する", class: "ml-5 btn btn-success" %></td>
        </tr>
      </tbody>
    </table>
  <% end %>
</div>